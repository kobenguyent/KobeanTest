{
  "name": "kobeantest-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "typecheck": "echo '✓ Workspace baseline ready'",
    "test": "node --experimental-strip-types --test packages/*/tests/*.test.js",
    "test:rust": "cd apps/desktop/src-tauri && cargo test"
  }
}
