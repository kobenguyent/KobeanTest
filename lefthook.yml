pre-commit:
  parallel: true
  commands:
    betterleaks:
      run: betterleaks scan --staged --config .betterleaks.toml
      tags: security
    typecheck:
      run: pnpm typecheck
      tags: quality

commit-msg:
  commands:
    conventional-commit:
      run: npx --yes commitlint --edit {1}
