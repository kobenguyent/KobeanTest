{
  "mcpServers": {
    "sqlite": {
      "command": "uvx",
      "args": ["mcp-server-sqlite@0.1.0", "--db-path", "target/mcp/kobean_dev.db"],
      "description": "Inspect and query local desktop SQLite FTS5 tables"
    },
    "chrome-devtools": {
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@0.4.0"],
      "description": "Inspect web UI layout, console logs, network requests, and run accessibility audits"
    }
  }
}
